version: 0.2

phases:
  install:
    commands:
      - echo "Checking environment..."
  build:
    commands:
      - echo "Build started on `date`"
      - echo "This is a simple test to verify CodeBuild permissions."
  post_build:
    commands:
      - echo "Build completed successfully!"

artifacts:
  files:
    - index.html
